name: CI - Escaneo de Vulnerabilidades (SecOps)

on:
  push:

jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Compilar AuraReviews
        working-directory: AuraReviews
        run: mvn compile -B

      - name: Escanear vulnerabilidades con OWASP
        working-directory: AuraReviews
        run: mvn org.owasp:dependency-check-maven:check -B

      - name: Subir reporte de vulnerabilidades
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: AuraReviews/target/dependency-check-report.html
          retention-days: 30
